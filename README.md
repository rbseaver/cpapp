# cpapp

This is a simple web-based interface for reviewing CPAP machine data. Initially, it will just be usable with the ResMed AirSense 11, which is my own CPAP machine. I will endeavor to write the code in such a way that support can be expanded later. I might fail, though, or get lazy. We'll see.